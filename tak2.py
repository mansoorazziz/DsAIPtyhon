# Given list of words
words = ["apple", "banana", "kiwi", "cherry", "mango"]

# Create dictionary mapping each word to its length
word_lengths = {word: len(word) for word in words}

# Display the result
print(word_lengths)
